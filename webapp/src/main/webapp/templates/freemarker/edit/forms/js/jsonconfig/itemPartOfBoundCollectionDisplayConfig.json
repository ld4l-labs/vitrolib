var displayConfig = {
    "fieldOrder":  ["objectVarLabel", "titleValue"],
    "fieldDisplayProperties": {
        "objectVarLabel": {
            "label": "Label (required)",
            "description": "Label for the bound collection (required). Recommended to be the title, if one exists; otherwise a cataloger-supplied value identifying or briefly describing the resource." 
        },
        "titleValue": {
            "label": "Title", 
            "description": "Title of the bound collection, if one exists." 
        }
    }
};